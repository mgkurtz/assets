# Assets for https://github.com/dandavison/delta/issues/2111

For the curious: the example was produced by searching for `rg lazy -g index.js` in my `~/.local/share/nvim/lazy` folder, containing various neovim plugins, including markdown-preview, which contains a JavaScript file.
